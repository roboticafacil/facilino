<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <Preference
        android:key="about_static_field_social_key1"
        android:persistent="false"
        android:selectable="true"
        android:summary="roboticaUPV"
        android:title="Facebook">
        <intent android:action="android.intent.action.VIEW" android:targetPackage="com.facebook.katana" android:data="http://m.facebook.com/roboticaUPV"/>
    </Preference>
    <Preference
        android:key="about_static_field_social_key2"
        android:persistent="false"
        android:selectable="true"
        android:summary="roboticaUPV"
        android:title="Twitter" >
        <intent android:action="android.intent.action.VIEW" android:data="http://twitter.com/roboticaUPV"/>
    </Preference>

</PreferenceScreen>
